const combine = (obj1, obj2) => ({...obj1, ...obj2});

let whiskeyTheDog = {
    name: 'Whiskey',
    species: 'canine',
    cool: true
 };

let KittyTheCat = {
    name: 'Kitty',
    species: 'feline',
    cool: false
};

function removeRandom(items) {
    let randIdx = Math.floor(Math.random()*items.length);
    let result = items.filter((el, idx) => idx !== items.findIndex(ind => ind === randIdx));
    return result;
}
